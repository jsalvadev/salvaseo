# Reglas del Proyecto

## Reglas Obligatorias

1. **NUNCA hacer commit sin autorización explícita del usuario.** El usuario debe dar permiso explícitamente antes de cada commit. No importa lo simple que parezca el cambio.