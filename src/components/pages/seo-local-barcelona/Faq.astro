---
import Upheading from '../../Upheading.astro'
import type { FaqItem } from '../../../data/faq-home'

interface Props {
  upheading?: string
  heading: string
  intro?: string
  items: FaqItem[]
}

const { upheading, heading, intro, items } = Astro.props
---

<div class='bg-white'>
  <div class='mx-auto max-w-7xl px-6 py-24 sm:py-32 lg:px-8 lg:py-40'>
    {upheading && <Upheading as='h2' text={upheading} />}
    <p class='text-4xl font-semibold tracking-tight text-petrol-900 sm:text-5xl'>
      {heading}
    </p>
    {intro && (
      <p class='mt-6 max-w-2xl text-base/7 text-petrol-700'>
        <Fragment set:html={intro} />
      </p>
    )}
    <dl class='mt-20 space-y-4'>
      {items.map((item, idx) => (
        <div class='bg-petrol-50 rounded-lg p-5 border border-petrol-100 lg:grid lg:grid-cols-12 lg:gap-8'>
          <dt class='text-base/7 font-semibold text-petrol-900 lg:col-span-5'>
            <h3>
              <span class='text-sky-500 font-mono text-sm mr-2'>{idx + 1}.</span>
              {item.question}
            </h3>
          </dt>
          <dd class='mt-4 text-base/7 text-petrol-600 lg:col-span-7 lg:mt-0'>
            <Fragment set:html={item.answer} />
          </dd>
        </div>
      ))}
    </dl>
  </div>
</div>
