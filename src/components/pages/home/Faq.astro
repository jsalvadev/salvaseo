---
import type { FaqItem } from '../../../data/faq-home'

interface Props {
  upheading?: string
  upheadingColor?: string
  heading: string
  idPrefix?: string
  items: FaqItem[]
  class?: string
}

const { upheading, heading, idPrefix = 'faq', items, class: className } = Astro.props

function stripHtml(html: string): string {
  return html.replace(/<[^>]*>/g, '').replace(/\s+/g, ' ').trim()
}

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: items.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: stripHtml(item.answer),
    },
  })),
}
---

<script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

<section class:list={['faq-root', className]}>
  <!-- Decorative watermark -->
  <div class="faq-watermark-wrap" aria-hidden="true">
    <span class="faq-watermark">?</span>
  </div>

  <div class="faq-inner">
    <!-- Header -->
    <header class="faq-header">
      {upheading && (
        <p class="faq-label">{upheading}</p>
      )}
      <h2 class="faq-heading">{heading}</h2>
    </header>

    <!-- FAQ list -->
    <dl class="faq-list">
      {items.map((item, i) => (
        <div class="faq-item">
          <dt class="faq-dt">
            <span class="faq-num" aria-hidden="true">
              {String(i + 1).padStart(2, '0')}
            </span>
            <button
              type="button"
              command="--toggle"
              commandfor={`${idPrefix}-${i}`}
              class="faq-btn"
            >
              {item.question}
            </button>
            <svg
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
              class="faq-chevron"
            >
              <path d="M19.5 8.25l-7.5 7.5-7.5-7.5" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </dt>
          <el-disclosure id={`${idPrefix}-${i}`} hidden class="contents">
            <dd class="faq-dd">
              <span aria-hidden="true"></span>
              <div class="faq-answer-body">
                <Fragment set:html={item.answer} />
              </div>
            </dd>
          </el-disclosure>
        </div>
      ))}
    </dl>
  </div>
</section>

<style>
  /* ── Root ─────────────────────────────────────────────── */
  .faq-root {
    position: relative;
    overflow: hidden;
    background-color: #EEF3F5; /* petrol-100 — soft cool off-white */
  }

  /* ── Watermark ─────────────────────────────────────────── */
  .faq-watermark-wrap {
    pointer-events: none;
    position: absolute;
    top: -2rem;
    right: -1rem;
    overflow: hidden;
    user-select: none;
  }

  .faq-watermark {
    display: block;
    font-family: 'Titan One', cursive;
    font-size: clamp(10rem, 28vw, 26rem);
    line-height: 1;
    color: rgba(26, 60, 74, 0.06);
    opacity: 1;
  }

  /* ── Inner container ───────────────────────────────────── */
  .faq-inner {
    position: relative;
    margin: 0 auto;
    max-width: 80rem;
    padding: 6rem 1.5rem;
  }

  @media (min-width: 640px) {
    .faq-inner { padding: 8rem 1.5rem; }
  }

  @media (min-width: 1024px) {
    .faq-inner {
      padding: 10rem 2rem;
      display: grid;
      grid-template-columns: 1fr 1.6fr;
      gap: 0 5rem;
      align-items: start;
    }
  }

  /* ── Header ────────────────────────────────────────────── */
  .faq-header {
    max-width: 42rem;
    margin-bottom: 4rem;
  }

  @media (min-width: 1024px) {
    .faq-header {
      margin-bottom: 0;
      position: sticky;
      top: calc(var(--navbar-height, 80px) + 2rem);
    }
  }

  .faq-label {
    display: inline-flex;
    align-items: center;
    margin-bottom: 1.25rem;
    border-radius: 9999px;
    border: 1px solid rgba(249, 115, 22, 0.35);
    background: rgba(249, 115, 22, 0.1);
    padding: 0.25rem 0.875rem;
    font-family: var(--font-family-sans, 'DM Sans Variable', sans-serif);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #fb923c;
  }

  .faq-heading {
    font-family: 'Titan One', cursive;
    font-size: clamp(2.5rem, 5vw, 3.75rem);
    line-height: 1.05;
    color: #1A3C4A; /* petrol-900 */
  }

  /* ── FAQ list ──────────────────────────────────────────── */
  .faq-list {
    margin: 0;
    padding: 0;
    max-width: 48rem;
  }

  @media (min-width: 1024px) {
    .faq-list { max-width: none; }
  }

  /* ── FAQ item ──────────────────────────────────────────── */
  .faq-item {
    border-top: 1px solid rgba(26, 60, 74, 0.12);
    padding: 1.75rem 0;
  }

  .faq-item:last-child {
    border-bottom: 1px solid rgba(26, 60, 74, 0.12);
  }

  /* ── dt grid: [number | question | chevron] ────────────── */
  .faq-dt {
    display: grid;
    grid-template-columns: 3rem 1fr 1.5rem;
    align-items: start;
    gap: 0.75rem 1rem;
  }

  @media (min-width: 640px) {
    .faq-dt {
      grid-template-columns: 4rem 1fr 1.5rem;
      gap: 0.75rem 1.5rem;
    }
  }

  /* ── Number ────────────────────────────────────────────── */
  .faq-num {
    font-family: 'Titan One', cursive;
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    line-height: 1;
    padding-top: 0.1rem;
    color: rgba(26, 60, 74, 0.2); /* muted default on light bg */
    transition: color 0.3s ease;
  }

  .faq-item:has(button[aria-expanded="true"]) .faq-num {
    color: #f97316; /* orange-500 — active */
  }

  /* ── Button ────────────────────────────────────────────── */
  .faq-btn {
    display: block;
    width: 100%;
    text-align: left;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    font-family: var(--font-family-sans, 'DM Sans Variable', sans-serif);
    font-size: clamp(0.9375rem, 2vw, 1.125rem);
    font-weight: 600;
    line-height: 1.45;
    color: #1A3C4A; /* petrol-900 */
    transition: color 0.2s ease;
  }

  .faq-btn:hover {
    color: #fb923c;
  }

  .faq-item:has(button[aria-expanded="true"]) .faq-btn {
    color: #fb923c;
  }

  /* ── Chevron ───────────────────────────────────────────── */
  .faq-chevron {
    width: 1.125rem;
    height: 1.125rem;
    flex-shrink: 0;
    margin-top: 0.25rem;
    color: #3A6D82; /* petrol-600 */
    transition: transform 0.38s cubic-bezier(0.16, 1, 0.3, 1), color 0.3s ease;
  }

  .faq-item:has(button[aria-expanded="true"]) .faq-chevron {
    transform: rotate(180deg);
    color: #f97316;
  }

  /* ── Answer dd ─────────────────────────────────────────── */
  el-disclosure:not([hidden]) {
    animation: faq-reveal 0.42s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  @keyframes faq-reveal {
    from { opacity: 0; transform: translateY(-6px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .faq-dd {
    display: grid;
    grid-template-columns: 3rem 1fr;
    gap: 0 1rem;
    margin: 0.875rem 0 0;
    padding: 0;
  }

  @media (min-width: 640px) {
    .faq-dd {
      grid-template-columns: 4rem 1fr;
      gap: 0 1.5rem;
    }
  }

  .faq-answer-body {
    padding-right: 1.5rem;
  }

  /* Override hardcoded petrol-900 classes in set:html content (requires :global) */
  .faq-answer-body :global(p),
  .faq-answer-body :global(li) {
    color: #2A5566 !important; /* petrol-700 */
    line-height: 1.75;
    font-size: 1rem;
  }

  .faq-answer-body :global(strong) {
    color: #1A3C4A !important; /* petrol-900 */
    font-weight: 600;
  }

  .faq-answer-body :global(ul) {
    color: #2A5566 !important; /* petrol-700 */
    margin-top: 0.5rem;
  }

  /* ── Reduced motion ────────────────────────────────────── */
  @media (prefers-reduced-motion: reduce) {
    el-disclosure:not([hidden]) { animation: none; }
    .faq-chevron,
    .faq-num,
    .faq-btn { transition: none; }
  }
</style>
